#include<bits/stdc++.h>
using namespace std;
using ll = long long;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    long long m, n;
    cin >> m >> n;
    long long maxDomino;
    maxDomino = (m*n)/2;
    cout << maxDomino << endl;
    return 0;
}